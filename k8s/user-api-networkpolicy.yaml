apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: user-api
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/user-api: "true"
  podSelector:
    matchLabels:
      io.kompose.network/user-api: "true"
