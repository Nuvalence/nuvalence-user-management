apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: user-management
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/user-api: "true"
  podSelector:
    matchLabels:
      io.kompose.network/user-api: "true"