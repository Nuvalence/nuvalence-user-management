apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: localhost-pgadmin
  namespace: user-management
spec:
  ingressClassName: nginx
  rules:
  - host: localhost
    http:
      paths:
      - backend:
          service:
            name: pgadmin
            port:
              number: 5050
        path: /pgadmin
        pathType: Prefix